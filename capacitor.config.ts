import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mobile-angular-app',
  webDir: 'dist/mobile-angular-app',
};

export default config;
